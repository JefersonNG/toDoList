server {
  listen 80;
  server_name fipiphone.com.br www.fipiphone.com.br;

  location /.well-known/acme-challenge/ {
    root /var/www/html;
  }

  location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri /index.html;
  }
}